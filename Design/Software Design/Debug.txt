Debugging
---------------
1) [R] too much ram usage
    initially, both setup::planner and setup::hexa were in the compilation
    this effectively doubles up the RAM needed for setup::planner, which overshoots the RAM capabilities of the chip
        commenting out either object reveals this
    8192 bytes is RAM maximum, 5695 bytes is current RAM usage
    reduce float usage, increase pointer usage
    see if way to reduce Vectors...?
2) [R] timing and dynamic memory allocation of object declaration
    a) some objects need to be instantiated after Serial.begin() is called to ensure that the protocol works and does not crash the program
    b) there are two methods to solve this:
        i) heap allocation using the new keyword
             //mp = new MotionPlanner(g, duty_f, cycle_time, step_h);
             this is not preferable because memory used on the heap must bee freed by itself
        ii) static allocation of the object in setup (satisfying the timing and lifetime of the object) and a global pointer to the object
            MotionPlanner* mp = nullptr;
            void setup(){
                static MotionPlanner planner(g, duty_f, cycle_time, step_h); // statically stored for life of program
                mp = &planner; // connect pointer and static object
            }
3) [R] "a block-scope function may only have extern storage class"
    a) C++ is denoting this as a function delaration inside void setup(). this worked for MotionPlanner and Hexapod because () were filled with arguments
    b) solution is to remove () -> static CommunicationManager comm;
4) [R] C:\Users\E_VADE~1\AppData\Local\Temp\cc3mJ23C.ltrans0.ltrans.o: In function `CommunicationManager::radioISR()':
<artificial>:(.text+0x22c): undefined reference to `CommunicationManager::radio_interrupt'
    a) seems that radio_interrupt is not visible to radioISR(), but ONLY when attachInterrupt() is not called on the ISR (works with just the ISR declaration)
    b) radio_interrupt is declared private: volatile static bool radio_interrupt;
        solution for this specific problem was radio_interrupt could only be accessed by a class-member function, whereas initially the static radioISR() converts
        it to a global function. therefore, changing the architecture to have a "2-stage" approach lets the interrupt access the class function and adjust the member variable
5) [R] argument of type \"void (CommunicationManager::*)()\" is incompatible with parameter of type \"void (*)()\
    a) occurs when interrupt expects ISR to be global and not have an additional this, which occurs for non-static member functions
        void(*)(CommunicationManager*) is what the ISR received when the function was not static
        void(*)() is what the ISR received when static
        solution: static void radioISR() with handler function discussed above that can access the class object
            static functions cannot access members regardless of member visibility
6) [R] fix command received interrupt
    a) seems like radio_interrupt in CM class blocks rest of code from compiling/executing?
        solution: when radio is not connected, maskIQR and setStatusFlags both blocked the code from continuing. 
            check if this works with the radio actually connected to the MEGA2560
    b) [R] pin state is never actually pulled low from high (or vice versa, edge never seems to fall)
        potentially make something in just main.cpp to test IRQ w/ NRF
        seems like the code would hang everytime radio was called without a radio being initialized
        can setup IRQ on pin 2 and work the interrupter in the CM class with a button, but it remains to be seen if this pin works with the nrf (hardcoded)
            solution: looks like IQR pin needs to physically be connected - datasheet: pin 4
            radio pin needs to be reset after falling to LOW/0, by calling radio.whatHappened(bool...) which dictates which event triggered the int, and then resets pin to 1/HIGH
    Solution: move setStatusFlags(MASK) to commBegin so that the radio was initialized before trying to interface with it
        code hangs when it cant get the SPI comm to work since the slave device isnt initialized
        also need to PHYSCIALLY connect interrupt pin to NRF (resources made it seem like SPI would handle this)
7) [R] fix receiver continuosly outputting data even when there are no new commands
    a) TM code faster frequency than receive, so buffer (3 max) may get filled, but while(radio.available()) {radio.read(...)} should take care of this issue
        looks like there werent brackets around the read portion, so the while loops was doing its thing and then not reading new data
        latency should be fixed here now - test this
        solution: print conditions adjusted (only if new command received), if(newCommand) uncommented
8) [R] NRF send/rec packets not aligning, specifically float vx
    a) attempt to change to integral data types to verify sending the same message
        instead of using Vector twist {float...}, creating the packet with integral types only removes ambiguity
    b) see that sizeof(packet) sent and received were different (16 bytes/packet sent from ESP32, 15 bytes received/packet on mega 2560)
        esp32 processor was padding to make things even
            struct __attribute__((packed)) Packet to pack it down to 15 bytes, no padding
                byte size agrees, but vx is still -11.53 instead of 0, sometimes -12.03 or -12.53
    solution: the center_shift was not centered for the /- nonzero values and was making things negative that shouldnt have been
        also, the adc values when plugged in via the battery were different than the VCC pin, about 200 bits larger so the values changed
9) [R] vy is nonzero despite sin(theta) = 0
    sin(theta) was in fact not zero because the wrong digits of pi/3 were used, resulting in a small y component
10) [R] lb + rb turn -> shutdown
    lb rb simultaneously was not treated as its own state because the individual poweron/power off variables were still set. used && ! other flags to make it separate
11) [R] memory corruption on stack or heap?
    SRAM is allocated at compile time and accounts for:
        program variables:
            global/static variables
            class members
        heap (new, malloc, calloc, realloc)
            grows upward
            https://www.embeddedrelated.com/glossary/heap
        free space
        stack (grows downward)
            local variables at runtime
            function pointers
            return addresses
    Allocating everything at compile time in an embedded system is better so that there are no memory-leak surprises
    Using freeMemory() at various places into the deepest call-chain revealed there is still over 500 bytes of RAM free
12) [R] NRF / interrupt SPI failure
    interrupt is more than just set/calculate, it calls a handler, sets an IO, and uses SPI (radio.whatHappened) to determine the nature of the interrupt
        solution: this could cause multiple SPI calls on the same thread which may mess things up
13) [] body kinematics fails after second command velocity
    probably cause of way more issues than realized
    setStepHeight was overwriting the one-time set value of step_height at body construction. while this isnt the end of the world and has an intended use, the coordinate
    that needed to be changed with lift_z, so that the value could be used by ripple gait, and not change step_height with each call
    this was causing kinematic issues as the legs would try to go to places it could not physically compute
14) [R] ripple gait simplicity
    while the ripple gait happens in 6 steps (swing) -> (stance) with each leg at a phase offset of 1/6, it still needs the stance legs to move 1/5 the distance,
        because one of the moves in the cycle is used for swing, therefore leaving the other 5/6 phases to account for 5/5 of the stance
    was using a lot of ternary operators to accomplish "circular distance" to compute the necessary phase, then use the phase to find the stance %
        can use circular distance by computing the distance, then shifting the values forward using the mod divisor as the absolute value insurance
        was close to this, but not really sure why i didnt do it?
        d = (ripple_cnt - leg + NUM_LEGS) % NUM_LEGS; so that is leg > rc, the distance is still found
15) [R] ripple gait did not work in backward direction
    B_t.setX1(B_sw.getX1() - ((B_sw.getX1() - B_st.getX1())*(sf))*cos(theta)); <- cos(theta) was already applied thru
    the body velocity command, does not need to be added here to obtain a component in the X direction (the components are already purely X)
    adding cos(theta) made it so that on theta = pi, cos(pi) = -1 -> modifier was applied to swing variable twice, made elements out of range for kinematics
