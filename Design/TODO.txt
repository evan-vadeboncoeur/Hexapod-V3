TODO Priority (ALL)
-------------------
1) adjust RC schematic and pcb to match devkit, not wroom
2) rough CAD controller design
3) "trajectory generation"
    a) read chapter 1 and hex chapter in motion planning book to get a better idea of implementation (probably not 100% applicable due to no time, velocity, acceleration control for path planning)
4) calculate workspace limitations and maximum swing angles / J1/2 interaction monitoring
 

PCB/ECE TODO
----------
1) LM2596 pin measurements
2) test joystick continuties for Vx Vy, power, ground
3) test esp32 5V power via buck converter, probe 3v3 etc
4) setup esp-mega 2560 nrf test in two terminals
5) rough CAD controller design
6) order PCBs

CAD TODO
---------------------
1) make controller design
2) make a stronger chassis

Code TODO
----------
1) motion planner "trajectory generation"
    0) create template coordinates for each position in the body frame (treated as body frame = walking frame), (with adjustable linear advance magnitude)
        i) plant(LEG(i))
        ii) lift(LEG(i))
        iii) swing
        iv) push
        v) plant
    1) fill in template coordinates for magnitude
    2a) choose walking direction
        i) establish tripods, leg orientations (if using old version)
    
    generate global coordinates for each leg, depending on:
        i) leg
        ii) walking direction
    3a) convert to local coordinates for leg to understand
    4a) have leg purely take values into account, no other context
2b) dynamic linear advance calculations based on leg (dynamic in motion planner)


