TODO Priority (ALL)
-----------
1) test J2 mount angle, doesn't seem to be at what the kinematics expects at t3 = 0 (-90 degrees off)
2) make sure macro motion works
3) "trajectory generation"
4) Connector controller design
    a) ECE - layout, etc
    b) CAD
 

PCB/ECE TODO
----------
1) board design
    a) lay out components

CAD TODO
---------------------
1) make controller design
2) make a stronger chassis

Code TODO
----------
0) test macro functions
1) motion planner "trajectory generation"
    0) create template coordinates for each position in the body frame (treated as body frame = walking frame), (with adjustable linear advance magnitude)
        i) plant(LEG(i))
        ii) lift(LEG(i))
        iii) swing
        iv) push
        v) plant
    1) fill in template coordinates for magnitude
    2a) choose walking direction
        i) establish tripods, leg orientations (if using old version)
    
    generate global coordinates for each leg, depending on:
        i) leg
        ii) walking direction
    3a) convert to local coordinates for leg to understand
    4a) have leg purely take values into account, no other context
2b) dynamic linear advance calculations based on leg (dynamic in motion planner)


