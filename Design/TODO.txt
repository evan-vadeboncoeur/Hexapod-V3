TODO Priority (ALL)
-------------------
COMM
1) interrupt that watches the state of NRF output pin on BOTH devices, turns high then low (tf, rec)
2) verify RC -> robot interrupt
3) make angle clamp velocities better:
    seems like R_MAX is the current point of contention - need to differentiate between half velocity and MAX/MIN ADC
4) figure out how to get it so the command packet doesnt get seen as new on the rec arduino even tho one isnt sent
5) test hexapod class
6) solder 5.6k to robot
ROBOT
1) slot for NRF in body (fix along with removing weight, adding small radial bores for soldering iron...)
2) fix plastic servo horn (leg 5)
3) fix leg 2
4) re-zero legs
5) Calculate workspace limitations and maximum swing angles / J1/2 interaction monitoring
    need to work on leg interaction (J2 J3 collision monitoring)
6) J2 add rubber boot



PCB/ECE TODO
-------------
1) solder full ctl board
    add final components
        nrf
        big caps
        aux IO
3) solder full comm board (5.6k ohm in BAG)
5) test NRF communications!!!


CAD TODO
---------------------
L0
- weight removal in top/bottom box (wall cuts)
- cantilever on top joints is (probably) unwise -> add strut on top chassis for long links

L3
- rubber foot at base that is drilled/tapped

Code TODO
----------
1) test RC interaction
2) test hexapod class 
    state machine
    walking single up function
    startup, shutdown
    new twist
    zero twist





