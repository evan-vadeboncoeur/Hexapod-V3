TODO Priority (ALL)
--------------------
0) modify chassis upper
1) print chassis upper
2) rebuild robot (re-zero legs)
3) test hexapod class (after R2, R3, R4)
    need to fix something about lb + rb turn -> idle doing nothing
    fix non-fluid pause between steps (lessen the delay / MAKE CLASS VARIABLES TO STORE VALUE IN, SKIP CALC IF NOT NEW COM [add bool: new])
4) interrupt management
    a) received interrupt  
        i) figure out way to connect pin 2 to NRF pin 8... (IRQ)
            cutout pin 4 (and opp if needed)
            solder wire to board and NRF pin
            solder to pin D2?
        ii) test interrupt code
    b) interrupt that watches the state of NRF output pin on EPS32, input pin on 2560. pulls LED high, timer flag high for LED pin, then polling timer turns it off. 
        potentially use another interrupt for timer overflow -> led pin = off
5) solder in 8.2k, test battery voltage, throw out 2S??? (meantime: probe regularly)

ROBOT
3) continue programming ripple gait
4) ensure proper memory usage so that RAM doesnt top out -> remove unused arrays in motion planner class
5) slot for NRF in body (fix along with removing weight, adding small radial bores for soldering iron...) (also see if can screw in???)

MISC/LOW PRIORITY
1) Calculate workspace limitations and maximum swing angles / J1/2 interaction monitoring
    need to work on leg interaction (J2 J3 collision monitoring)
2) J2 add rubber boot


PCB/ECE TODO
-------------
1) solder full ctl board
    add final components
        nrf
        big caps
        aux IO
2) desolder 5.6k resistor
3) solder 3.3k resistor
4) test batter management
    comm
    robot

CAD TODO
---------------------
L0
- weight removal in top/bottom box (wall cuts)
- cantilever on top joints is (probably) unwise -> add strut on top chassis for long links

L3
- rubber foot at base that is drilled/tapped

Code TODO
----------
1) verify ripple gait
2) program wave gait





