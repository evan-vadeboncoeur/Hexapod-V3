TODO Priority (ALL)
--------------------
) seems like first command ok, second is completely bananas - why?
    test two commands in MP class, same magnitude, different direction -> plot and compare  
        if ok, timing or gait order is somehow out of wack
            otherwise, it's probably the body kinematics being overwritten/not using the proper idle function
) test hexapod class
    fix non-fluid pause between steps (should only recalculate on new command)
        is delay(100) enough to ruin the fluidity of the robot in half tripod?
            should probably
) seems like turn command works well only in planner, not state machine , timing not great
    make a time estimate function that takes angle delta into consideration, omega, uses millis()
) fix ripple (why does sixths with non-ripple work [0 4 2, 5 1 3] but fifths with non-ripple doesnt)
    also, seems like stance math is off for reverse (so there is a negative where it is not supposed to be. plot and compare, make new equation)
    phase = (rc + i)%NUM_LEGS
    S = phase / 5.0;
) test hexapod->startup to make sure motors go to storage pose, not 0 pose - continue here
) solder in 8.2k, test battery voltage function (meantime: probe regularly)
) fix startup -> belly flop (should be spaced into tripods...? has macro delay in it)
) potentially skip idle -> home before going to storage in macro
)interrupt that watches the state of NRF output pin on EPS32, input pin on 2560. pulls LED high, timer flag high for LED pin, then polling timer turns it off. 
    potentially use another interrupt for timer overflow -> led pin = off
) add slat to bottom chassis for battery (dovetail in and out - still have hex cuts)

ROBOT
3) continue programming ripple gait

MISC/LOW PRIORITY
1) J2 add rubber boot
2) workspace limitations

PCB/ECE TODO
-------------
1) test battery management
    robot

CAD TODO
---------------------
L0
- weight removal in top/bottom box (wall cuts)
- cantilever on top joints is (probably) unwise -> add strut on top chassis for long links

L3
- rubber foot at base that is drilled/tapped

Code TODO
----------
1) improve ripple gait
2) program wave gait





